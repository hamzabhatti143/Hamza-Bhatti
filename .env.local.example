# RAG backend URL (used by visitor-chat and admin-chat API routes)
RAG_BACKEND_URL=http://localhost:8000

# Admin panel — server-side password validation (keep this secret)
ADMIN_PASSWORD=Hamza2004@

# Admin panel — client-side password gate (must match ADMIN_PASSWORD)
NEXT_PUBLIC_ADMIN_PASSWORD=Hamza2004@
